name: carzone_demo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  google_fonts: ^6.1.0
  get_it: ^8.0.3
  dio: ^5.8.0+1
  provider: ^6.1.5
  dartz: ^0.10.1
  encrypt_shared_preferences: ^0.9.9
  todo: ^2.0.1
  

flutter:
  uses-material-design: true

  assets:
    - images/omar.jpg
